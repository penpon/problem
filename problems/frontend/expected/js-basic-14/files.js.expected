document.addEventListener('DOMContentLoaded', () => {
  const link = document.getElementById('dangerLink');
  const status = document.getElementById('status');
  let count = 0;

  link.addEventListener('click', (e) => {
    e.preventDefault();
    count++;
    status.textContent = `${count} 回防止しました`;
  });
});
