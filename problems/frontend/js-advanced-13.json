{
  "id": "js-advanced-13",
  "title": "商品検索（キーワード絞り込み）",
  "description": "map() で一覧を描画し、入力値で filter() + includes() によるキーワード検索を実装します（大小無視）。",
  "category": "javascript-advanced",
  "instructions": [
    "一覧描画: map() + join('') + innerHTML",
    "検索: toLowerCase() + trim() → filter() + includes()"
  ],
  "implementationPoints": [
    "必須: includes() を使う",
    "必須: 大小無視（toLowerCase 使用）"
  ],
  "files": { "html": {"template": "", "expected": ""}, "css": {"template": "", "expected": ""}, "js": {"template": "", "expected": ""} },
  "expectedResult": { "behavior": "入力した語を含む商品だけ表示される" },
  "checks": [
    { "id": "uses-includes", "name": "includes()使用", "type": "js", "message": "includes() を使用してください" }
  ],
  "hints": [
    "'ノート'.includes('ノ') → true",
    "'ABC'.toLowerCase() → 'abc'",
    "''.trim() は空文字のままです"
  ]
}
